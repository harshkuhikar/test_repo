// // Animate project elements on page load
// const projects = document.querySelectorAll('.project');
// projects.forEach((project, index) => {
//   gsap.to(project, { opacity: 1, y: 0, delay: index * 0.2 });
// });
